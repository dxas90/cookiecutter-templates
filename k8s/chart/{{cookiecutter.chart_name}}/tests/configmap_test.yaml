suite: test configmap
templates:
  - configmap.yaml
tests:
  - it: should create ConfigMap
    asserts:
      - isKind:
          of: ConfigMap
      - equal:
          path: metadata.name
          value: RELEASE-NAME-{{ cookiecutter.chart_name }}-config
      - equal:
          path: metadata.labels["app.kubernetes.io/part-of"]
          value: {{ cookiecutter.chart_label }}

  - it: should include correct labels
    asserts:
      - isSubset:
          path: metadata.labels
          content:
            app.kubernetes.io/name: {{ cookiecutter.chart_name }}
            app.kubernetes.io/instance: RELEASE-NAME

  - it: should have data section
    asserts:
      - isNotNull:
          path: data
